<%= form_with(model: seedbox) do |form| %>
  <% if seedbox.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(seedbox.errors.count, "error") %> prohibited this seedbox from being saved:</h2>

      <ul>
        <% seedbox.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :name, style: "display: block" %>
    <%= form.text_field :name %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
