<%= form_with(model: bin) do |form| %>
  <% if bin.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(bin.errors.count, "error") %> prohibited this bin from being saved:</h2>

      <ul>
        <% bin.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :seed_id, style: "display: block" %>
    <%= form.collection_select :seed_id, Seed.all, :id, :species %>
  </div>

  <div>
    <%= form.label :seedbox, style: "display: block" %>
    <%= form.text_field :seedbox %>
  </div>

  <div>
    <%= form.label :location, style: "display: block" %>
    <%= form.number_field :location %>
  </div>

  <div>
    <%= form.label :quantity, style: "display: block" %>
    <%= form.number_field :quantity %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
